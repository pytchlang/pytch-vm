Remove the variable watcher for the variable called ``variable_name``.
It is not an error if there is not currently such a watcher.  (There
is a more general version available; see :ref:`its
documentation<pytch_variable_watchers>` for details.)
