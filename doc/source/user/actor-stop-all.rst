Stop all currently-executing scripts.  Also stop all sounds, delete
all clones, abandon all "ask and wait" questions, and clear all speech
bubbles.

``stop_all()`` does the same job as the "red stop" button.
