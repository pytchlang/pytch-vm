To set up for writing docs:

    python3 -m venv venv
    source venv/bin/activate
    pip install sphinx sphinx-rtd-theme sphinx-autobuild

Then

    sphinx-autobuild --re-ignore '/\.#' source build/html

will launch a live-reload server to view the results.
